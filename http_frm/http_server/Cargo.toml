[package]
name = "http_server"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.0-rc.1"
#rocket_prometheus = "0.9.0"
rocket_prometheus = { path = "../../../rocket_prometheus" }


[lib]
name = "http_server"
path = "src/http_server.rs"